// Optional production override. Load this before api-client.js if the API is
// deployed on a different origin than the static frontend.
// window.NEXARVIA_API_BASE = "https://your-api.example.com";
